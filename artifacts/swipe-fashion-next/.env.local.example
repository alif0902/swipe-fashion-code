# Transaction pooler (port 6543) — dipakai aplikasi saat runtime.
# Wajib pooler, bukan direct, karena Vercel berjalan serverless.
DATABASE_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres"

# Direct connection (port 5432) — hanya dipakai drizzle-kit push dan seed.
DIRECT_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-region.pooler.supabase.com:5432/postgres"

# Untuk pengembangan lokal dengan Postgres dari Homebrew, keduanya sama:
# DATABASE_URL="postgresql://USER@localhost:5432/swipefash"
# DIRECT_URL="postgresql://USER@localhost:5432/swipefash"

# Kunci penandatangan sesi login. Buat nilai acak sendiri:
#   openssl rand -base64 32
# Wajib diisi juga di Vercel (Settings -> Environment Variables); kalau tidak,
# build produksi gagal dengan pesan dari lib/auth.ts.
BETTER_AUTH_SECRET="ganti-dengan-hasil-openssl-rand-base64-32"

# Opsional. Kosongkan saja — di Vercel URL deployment dipakai otomatis, dan di
# lokal Better Auth memakai origin permintaan.
# BETTER_AUTH_URL="http://localhost:20100"

# Tempat menyimpan gambar yang diunggah: foto produk dari panel admin dan foto
# profil pengguna. Keduanya lewat lib/storage.ts.
#
# WAJIB kalau kamu ingin menambah produk berfoto. Tanpa ini, 商品追加 gagal
# dengan pesan「画像の保存先が未設定です」— dan itu memang disengaja, karena
# menyimpan gambar ke disk tidak mungkin di Vercel yang serverless dan
# filesystem-nya read-only.
#
# Cara memperolehnya:
#   1. Buka project di vercel.com -> tab Storage -> Create -> Blob
#   2. Setelah store dibuat, buka tab .env.local di halaman store itu
#   3. Salin nilai BLOB_READ_WRITE_TOKEN ke sini
#
# Di Vercel sendiri variabel ini disuntikkan otomatis begitu Blob store
# terhubung ke project — tidak perlu ditambahkan manual di Environment
# Variables.
#
# Token ini memberi akses TULIS ke penyimpananmu. Jangan pernah di-commit.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_XXXXXXXXXXXX_XXXXXXXXXXXXXXXXXXXXXXXX"
